<VirtualHost *:80>
    ServerAdmin ${SERVER_ADMIN}

    DocumentRoot ${DOCUMENT_ROOT}

    <Directory "${DOCUMENT_ROOT}">
        Options +FollowSymLinks
        RewriteEngine on
        AllowOverride all
    </Directory>
</VirtualHost>
